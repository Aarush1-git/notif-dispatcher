version: "3.8"
services:
  rabbitmq:
    image: rabbitmq:3.13-management
    ports:
      - "5672:5672"    # AMQP protocol port - your app connects here
      - "15672:15672"  # web UI - open this in a browser